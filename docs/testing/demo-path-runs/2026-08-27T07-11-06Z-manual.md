# Demo Path Run — 2026-08-27T07:11:06Z (manual walkthrough)

Manual cross-feature walkthrough required by T016 (FR-001, spec AS1-1). Unlike the
script-generated runs in this directory, every step here was performed **by hand through the
browser UI** at `http://localhost:5173` on the demo machine, in one continuous run, with no
restart and no hand-edited data between stages. This is the human counterpart to the
release-gated `demo-path` script run of 2026-08-27T04:58:25.974Z — it exercises the same
seven legs through the actual employee and staff screens rather than the HTTP surface.

**Started**: 2026-08-27T07:11:06Z
**Finished**: 2026-08-27T07:19:25Z
**Ticket**: HD-0053
**Result**: PASS — all 7 legs, with 2 UI defects observed (OBS-11, OBS-12); neither blocked
the journey.

## Stack under test

Real demo-machine stack, unmocked:

- MongoDB single-node replica set `rs0` (`mongodb://127.0.0.1:27017/helpdesk?replicaSet=rs0`)
- LM Studio, `qwen2.5-7b-instruct`, OpenAI-compatible API on `http://127.0.0.1:1234/v1`
- Registered SSH test endpoint **Test Node B**
- Backend health at start: `{"status":"ok","llm":{"reachable":true,"model":"qwen2.5-7b-instruct"},"db":{"reachable":true}}`

Accounts: `demo-path-employee@local.test` (reporter), `demo-path-staff@local.test` (staff).

## Stage outcomes

| # | Leg | Status | Detail |
|---|---|---|---|
| 1 | Text intake | PASS | Signed in as Demo Path Employee; submitted "Nothing is coming out of the shared printer in the finance room and the queue seems stuck." The duplicate check fired against the still-open HD-0049 and asked whether this was the same problem; answered "This is something new, a different printer." and the flow continued — the check behaved correctly against a genuine near-duplicate |
| 2 | Classification | PASS | Classified as **Printer**, confidence 80%, handling mode `automated` — no clarification round needed |
| 3 | Ticket creation | PASS | HD-0053 created and its reference surfaced to the reporter in-conversation ("Your ticket reference is HD-0053. You can quote this any time.") |
| 4 | Guided troubleshooting | PASS | Step 1 of 3 offered ("Check that the printer is powered on and has paper and ink/toner."); answered "Yes it is powered on and connected, but still nothing prints."; the step was recorded as **not worked** and the guide advanced to step 2 of 3 ("Cancel any stuck print jobs from the print queue, then try printing again.") |
| 5 | Consent + escalation | PASS | The consent block offered the whitelisted action against Test Node B and named it as a test system, not the reporter's own device; clicked "Yes, go ahead"; the agent correctly withheld execution — "That needs IT staff sign-off first" — and moved the ticket to `pending_approval`. Consent recorded 07:13:22Z. Wording defects OBS-11 and OBS-12 were observed at this stage |
| 6 | Staff takeover | PASS | Signed out, signed in as Demo Path Staff; dashboard showed the pending-approval badge (Approvals 1) and HD-0053 in the list. Took over the ticket at 07:17:22Z: badge changed to **Escalated**, assignment to "Handled by Demo Path Staff", and History recorded `Handling changed from automated to human_involved · by staff`, with matching Staff activity and Assignment history entries |
| 7 | Whitelisted remediation | PASS | `/staff/approvals` showed the queued action with its target, script path and the reporter's consent timestamp. Approve raised a second explicit confirmation ("This will run /usr/local/bin/clear-print-queue.sh against Test Node B") before anything executed. Confirmed at 07:18:15Z: the action ran against Test Node B and returned **Succeeded**, output `cleared=test-printer`. The queue then read "Nothing is waiting on you right now", the ticket's Automated actions panel showed the record with "Consent given … · Approved by Demo Path Staff", and the outcome was reported back to the reporter in-conversation |

## Verdict

**PASS.** The full cross-feature journey completes by hand, end to end, on the real stack.
The two-gate safety control (reporter consent, then independent staff approval, then a third
explicit confirmation before execution) held at every step — at no point could the agent
execute a state-changing action on its own — and the audit surfaces (History, Staff activity,
Assignment history, Automated actions) each recorded their part of the run.

Two employee-facing wording/UI defects were found and are filed as OBS-11 and OBS-12 in
`docs/testing/observations.md` with their severity recorded **before** any fix (FR-004, V4.4).
Neither prevented any stage from completing, so neither gates this run.

## Note on interaction method

Clicks were dispatched through the page's own DOM handlers (browser automation), not
synthesised OS-level mouse events — screenshot capture was timing out on this machine, so the
run was observed via page text and network responses rather than images. Every stage
transition was independently confirmed against the rendered UI and the corresponding API
response, so the evidence above is the system's real state, not an assumed one. T021/T022
remain the tasks that produce the screenshot artifacts, and are correctly deferred until
after Phase 6.
